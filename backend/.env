POSTGRES_SERVER='localhost'
POSTGRES_PORT=5432
POSTGRES_USER='postgres'
POSTGRES_PASSWORD='1587'
POSTGRES_DB='task_manager'
POSTGRES_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_SERVER}:${POSTGRES_PORT}/${POSTGRES_DB}

REDIS_HOST=localhost
REDIS_PORT=6379

# Can use token_hex from secrets import, for now it's what we want
JWT_SECRET=secret
JWT_ALGORITHM=HS256